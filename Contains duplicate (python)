class Solution:
    def containsDuplicate(self, nums):
       array = set()
       for num in nums:
        if num in array:
            return True
        else:
        
            array.add(num)
       
       return False
       
       seeker = Solution()

       nums = [1,2,3,1]
       print(solution.containsDuplicate(nums))

       nums= [1,2,3,4]
       print (solution.containsDuplicate(nums))
